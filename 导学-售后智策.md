# CarePilot（售后智策）项目导学

> 历史状态：本文是 2026-09-11 的学习快照，部分样本量、发布和优先级已过期。保留它用于理解当时决策；当前版本请阅读 [`导学-CarePilot.md`](./导学-CarePilot.md) 与 [文档总索引](./docs/README.md)。

> 本文按 2026-09-11 工作区现状整理。它区分“计划”“可运行实现”“真实模型实测”和“模拟/离线证据”；不把三者混写为生产能力。

## 0. 先用一句话理解项目

CarePilot 是一个面向电商售后团队的**受控 Agent 协同处置工作台**：模型负责理解诉求、调用受限的订单/物流/政策/凭证工具并生成带政策依据的方案；确定性代码负责状态机、权限、人审、审计和身份边界。低风险解释、补件、任务与通知可以自动推进；退款、退货、换货、补偿和库存相关动作只能成为 Proposal，必须人工确认。

它与 Wayly 的组合是：Wayly 证明“可靠的消费者决策推荐”，CarePilot 证明“可靠地把 AI 嵌入企业工作流并保留人机协同”。

## 1. 项目现状：先分清四个层次

| 层次 | 当前事实 | 不能据此声称什么 |
| --- | --- | --- |
| 产品目标 | `PLAN.md` 定义 Consumer、Ops、Admin 三端和售后闭环 | 计划中的组件不自动等于完成 |
| 可运行系统 | React/Vinext 前端、FastAPI、SQLAlchemy、Supabase Auth/RBAC、迁移 SQL、受控模拟业务工具 | 模拟订单/物流不是真实电商系统集成 |
| 真实 Agent | 服务端真实文本/视觉模型、结构化输出、8 轮受限 Tool loop、真实运行持久化；Stage 16、18–20 记录了脱敏实测 | 不是线上生产服务或真实客服效率证明 |
| 评测与研究 | 80 条离线固定工单、20 张真实视觉冻结图、15 条真实模型 v0.2.2 评测、20 条匿名模拟 Manual vs Agent 任务记录 | 小样本模拟实验和冻结集不外推 GMV、留存、真实效率或法律适用性 |

**当前最可信的实测结论**：Stage 20 的 `real-agent-eval-v0.2.2` 是一轮 15 样本、`REAL_AGENT_RUN` 的受控脱敏技术评测。它记录 Policy Retrieval 100%、必需 Tool 调用 100%、高风险安全阻断 10/10、实际高风险 Proposal 的人审接管 5/5；高风险验收为 4/6。它仍是一次固定模型、Prompt、政策快照和测试集下的技术结果，不是生产 KPI。

## 2. 前置知识（面试高频）

| 知识点 | 为何需要 | 在本项目中的位置 | 高频度 |
| --- | --- | --- | --- |
| 有限状态机与幂等 | 避免 Agent 直接、重复或非法改变工单 | `domain.transition`、`process_ticket`、工单 API | 高 |
| LLM structured output / Tool calling | 把“生成文本”变为可校验的处理方案与工具调用 | `agent_runtime.py`、`ResolutionOutput` | 高 |
| 风险分级与 Human-in-the-loop | 将不可逆权益操作留给人工 | `permission_for_resolution`、review/take-over API | 高 |
| RAG/检索证据 | 方案须引用已发布政策，而不是模型自行编造规则 | `search_active_public_policy`、Policy Admin | 高 |
| 多模态 Evidence | 图片只提取可见事实，不裁决资格 | `analyze_image`、`evidence_review_gate` | 中高 |
| 鉴权与 RBAC | 前端隐藏入口不是权限控制 | `auth.py`、`008_supabase_auth_rbac.sql` | 中高 |
| 冻结评测与版本化 | 让数据、模型、Prompt、政策可重跑、可比较 | `evals/fixtures/`、`RealEvaluationRun` | 高 |

## 3. 重点亮点与学习顺序（先看这个）

| 亮点标题 | 为什么重要 | 通用技术关键词 | 先看哪些文件 | 建议学习顺序 |
| --- | --- | --- | --- | --- |
| 受限业务闭环 | 证明系统能改变工单状态，而非只聊天 | Agentic workflow、状态机、幂等 | `PLAN.md`、`apps/api/app/domain.py` | 1 |
| 风险与人工门禁 | 明确 AI 可以做什么、绝不能做什么 | Permission engine、HITL、fail closed | `domain.py`、`apps/api/tests/test_ticket_flow.py` | 2 |
| 真实模型运行可追溯 | 每次真实运行保留模型、Prompt、工具、证据、成本 | structured output、trace、observability | `agent_runtime.py`、`models.py` | 3 |
| 政策版本生命周期 | Candidate 不能绕过回归和人工发布成为 Active | versioning、regression gate、rollback | `data/005_public_policy_grounding.sql`、`domain.py` | 4 |
| 图片的两层安全判断 | 区分视觉模型原始判断与确定性业务门禁 | multimodal evidence、defense in depth | `data/010_evidence_review_gate.sql`、`domain.py` | 5 |
| 证据分级评测 | 用同一标准解释离线模拟、真人模拟、真实模型评测 | frozen eval、bad case、experiment design | `evals/README.md`、`docs/worklog/` | 6 |

## 4. 建议阅读路径

| 主题 | 通用技术点 | 建议阅读位置 | 预计时间 | 读完能回答什么 |
| --- | --- | --- | --- | --- |
| 产品边界 | 为什么不是通用聊天机器人 | `PLAN.md` | 25 分钟 | 为什么选择售后、三端如何分工、哪些能力明确不做 |
| 演进时间线 | 从本地原型到真实评测的事实变化 | `docs/worklog/README.md`、阶段 1、16、18、19、20 | 35 分钟 | 哪些是历史记录，当前真实结果是什么 |
| 核心处置链路 | 状态、Tool、Proposal、人工复核 | `apps/api/app/domain.py` 的 `process_real_ticket` | 45 分钟 | 一张真实工单如何从 NEW 到 RESOLVED / NEED_INFO / WAITING_REVIEW |
| 模型运行时 | schema、tool loop、失败封闭 | `apps/api/app/agent_runtime.py` | 35 分钟 | 如何限制 Tool、为什么 DeepSeek 需要重放上下文 |
| 数据模型 | 事实源与审计颗粒度 | `apps/api/app/models.py`、`data/001`–`010` | 30 分钟 | 为什么要持久化 AgentRun、EvidenceAsset、Policy snapshot |
| 政策和图片 | grounding、Evidence、人工门禁 | `evals/README.md`、Stage 13、17、20 | 30 分钟 | 为什么公开政策不是法律结论，图片为什么不能自动换货 |
| 评测 | 离线/真人模拟/真实模型的区别 | `evals/manual-agent-results-v0.2.md`、Stage 18–20 | 35 分钟 | 哪些数字可讲、哪些数字绝不能讲 |

## 5. 必备知识点 checklist

- [ ] 说清 `NEW → PROCESSING → NEED_INFO / WAITING_REVIEW / RESOLVED / FAILED` 的业务含义与非法跳转处理。
- [ ] 能解释 LLM 输出为何需要 Pydantic schema，Tool 名为何要通过白名单和服务端执行器。
- [ ] 能解释“高风险 Proposal”与“高风险 execute”是两件事：前者可生成，后者不存在于 Agent 可调用集合。
- [ ] 能解释 Policy 的 `Candidate → Regression → 人工 Publish → Active` 与回滚。
- [ ] 能解释 `model_needs_human_review` 和 `needs_human_review` 的区别。
- [ ] 能解释 80 条离线 fixture、5 人匿名模拟实验、15 条真实模型冻结评测分别能和不能证明什么。
- [ ] 能解释 Supabase Auth 的 JWT 与 `profiles` 角色表如何形成服务端权限控制。

## 6. 核心原理解析

### 6.1 从“回答问题”到“受控处置”

**问题**：客服不缺一段泛化话术，缺的是核对订单、物流、政策、凭证和更新工单的连续工作。  
**机制**：模型只能调用 `order.lookup`、`logistics.track`、`policy.search`、`evidence.read`、`ticket.history` 五个受限读取工具，并生成 schema 校验的 Resolution。  
**落点**：`apps/api/app/agent_runtime.py` 与 `domain.py` 中的 `_agent_tool_executor`；写入状态和通知不由模型直接调用。

### 6.2 模型生成与业务权限解耦

**问题**：Prompt 不能成为退款、换货等权益操作的最后防线。  
**机制**：模型只输出 `refund.propose`、`replace.propose` 等建议；`permission_for_resolution` 由确定性代码返回 `AUTO_EXECUTE`、`REQUIRE_HUMAN` 或 `BLOCK`。  
**落点**：高风险 Proposal 转为 `WAITING_REVIEW`；客服在 Ops 端选择同意、修改、不采用或接手，运行时不存在高风险 execute Tool。

### 6.3 证据的“两层判断”

**问题**：视觉模型可能把图像看得过于乐观；反过来，图片与订单不匹配也不是纯模型识别错误。  
**机制**：保存模型原始 `model_needs_human_review`，再由图片质量、订单/诉求匹配、置信度和必要可见字段计算确定性 `needs_human_review` 与原因。  
**落点**：Stage 20 新增 `review_gate_reasons_json`；Evidence 充分也只支持 Proposal，不触发自动履约。

### 6.4 政策依据的受控发布

**问题**：新来源、解析错误或回归失败不能立刻改变正在处理工单的规则依据。  
**机制**：受控来源导入快照、校验 checksum、生成 Candidate、跑回归，最后由管理员发布；查询只面向 Active 快照，并随运行保存 citation。  
**落点**：`PolicySourceSnapshot`、`PolicyGroundingChunk`、`PolicyVersion` 和对应的 Policy Admin API。

### 6.5 冻结评测让改动可比较

**问题**：如果每次模型、Prompt、政策和样本都变，任何“优化”都无法归因。  
**机制**：离线 80 条 case、视觉图哈希和真实评测的模型/Prompt/政策快照分别固定；改样本或配置必须建新版本。  
**落点**：`real-agent-eval-v0.2.2.json` 与 `RealEvaluationRun` 会持久化 `fixture_set`、`model_id`、`prompt_version`、`policy_snapshot`、指标和坏例。

## 7. 关键设计决策

| 决策 | 备选 | 取舍 | 风险 | 验证 |
| --- | --- | --- | --- | --- |
| 单一受限 Agent | Multi-Agent 编排 | 先完成单 Agent + 有界工具链；不把 Multi-Agent 当卖点 | 模型循环或工具重复 | 8 轮上限、重复成功调用检查、真实评测 |
| 权益操作只人审 | Agent 直接写退款/换货 | 只产生 Proposal | 自动化比例较低 | 高风险安全阻断、Proposal 接管率 |
| 受控政策包 | 前端任意抓 URL | 服务端受控清单与人工发布 | 覆盖范围有限、非法律适用证明 | checksum、片段回归、来源审计 |
| 视觉 + 确定性门禁 | 视觉模型单独决定 | 原始判断与业务门禁并存 | 仍需配对样本评估 | Stage 20 配对图片观察与坏例 |
| 三类评测分层 | 用一组漂亮汇总数字 | 分别标记离线模拟、匿名模拟、真实模型 | 不可直接外推生产 | fixture/运行 ID/原始导出可追溯 |

## 8. 当前可引用的量化与验证

| 项目 | 可准确表述 | 证据性质 |
| --- | --- | --- |
| 离线固定评测 | 80 条自构造模拟工单，数码/服饰/家居为 32/24/24；含 20 高风险和 12 异常 | 固定输入与确定性契约验证 |
| Manual vs Agent | 5 个匿名槽位、20 条完全模拟任务中，Agent-assisted 平均记录用时 18.3 秒，Manual 为 21.7 秒；两组均 10/10 达到冻结目标 | 小样本描述性模拟观察，不代表真实客服效率 |
| Stage 20 真实模型评测 | 15 个脱敏冻结样本；Policy Retrieval 与必需 Tool 调用均为 100%，高风险安全阻断 10/10，实际高风险 Proposal 的人审接管 5/5 | 一轮真实模型技术评测，固定模型/Prompt/政策快照/样本 |
| 代码回归 | 最新工作日志记录后端 22 条单元测试、Web production build 通过 | 本地技术验证；应在每次改动后重跑 |

## 9. 自学提醒

若某文件或原理看不懂，请继续追问 AI；本技能负责给学习路径与题目，不提供逐行讲解。

## 10. 项目技术定位

**AI 产品 + 后端工作流 + 数据评测的交叉项目。** 产品上重点是售后处置、三端协同和风险边界；技术上重点是有状态的 Agent Tool loop、服务端权限、可追溯证据与冻结评测，而不是追求“多 Agent”或“RAG”名词堆叠。
