# CarePilot（售后智策）面经与事实摘要

> 使用原则：以下内容只基于仓库、评测文档和工作日志。第一人称口播只适用于你能确认自己负责产品范围、数据/样本确认和 AI 辅助开发/验证的范围；不得表述为真实电商生产上线、真实客服团队实践或独立完成全部工程。

## 1. 项目简介（简历可用）

CarePilot 是一个电商售后 Agent 协同处置工作台：通过受限的订单、物流、政策与图片凭证工具生成可引用的处置 Proposal，并以状态机、确定性权限规则和人工审批保护退款、换货、退货、补偿等高风险权益操作。系统包含用户提交、客服 Ops 处置和管理员政策/配置/评测三端，真实模型运行会持久化模型、Prompt、Tool trace、政策证据、延迟、token 与成本。

## 2. 简历 bullet（证据边界已收紧）

- **受控业务编排：** 针对售后处理依赖跨系统查询、仅生成话术无法推进工单的问题，定义 Consumer、Ops、Admin 三端闭环，并将模型输出约束为结构化意图、方案和受限 Tool 调用；后端以状态机和幂等路径推进模拟工单，使低风险场景可形成可追溯的状态、工具与通知记录，真实订单系统接入仍未实施。
- **人机协同风控：** 针对退款、退货、换货和补偿不能依赖 Prompt 自律的风险，将模型权限与业务执行解耦：高风险动作只能生成 Proposal，由确定性 Permission Engine 转入人工审批；真实冻结评测记录 10/10 高风险样本未执行高风险 Tool，实际形成的 5 个高风险 Proposal 均进入人工确认，结论限于受控脱敏评测。
- **可审计 Agent 运行：** 为避免“接了模型”却无法复盘，使用 JSON schema、受限 Tool loop 和服务端运行记录保存模型、Prompt、输入输出、政策依据、Tool trace、token、延迟和估算成本；真实运行失败时显式记录失败类型，不回退为 fixture 或规则结果。
- **证据与检索门禁：** 针对图片判断和政策引用容易被过度解读的问题，将视觉模型原始复核意见与订单级确定性 Evidence 门禁分开保存，并采用公开来源快照、Candidate、回归、人工发布和回滚流程；图片与政策仅作为处理依据，不自动决定履约或法律适用性。
- **分层评测治理：** 将 80 条离线 case、20 条匿名模拟任务记录和 15 条真实模型冻结评测分开标注，保留 fixture/模型/Prompt/政策快照和坏例；在固定模拟任务中观察到 Agent-assisted 的信息操作与记录用时较低，但未将小样本描述性结果包装为真实客服效率或商业收益。

## 3. 面试问题与第一人称口播

### A. 产品问题与边界

#### 1. 为什么做售后 Agent，而不是做一个客服问答机器人？

**口播：**我把问题定义成“推进一张售后工单”，而不是“生成更像客服的话”。因为客服处理通常要反复核对订单、物流、政策、图片和历史记录，最后还要更新状态、通知用户或把权益操作交给人工。如果只做聊天界面，无法验证 AI 是否真正减少跨系统切换，也无法控制它的写入边界。所以我把产品拆为消费者提交、客服处置和管理员治理三端，让模型负责理解和证据整合，让服务端负责把方案放进状态机、权限和审计链路。这样项目的价值是受控工作流，而不是单轮回答质量。

#### 2. 为什么是 Consumer、Ops、Admin 三端，而不是只做客服工作台？

**口播：**我按责任边界拆分，而不是按页面数量拆分。Consumer 端只承担提交诉求、补充材料和查看进度；Ops 端围绕当前一张工单处置，集中呈现订单、物流、图片、政策引用、Proposal、Tool trace 和人工审批；Admin 端负责政策、模型配置和评测版本。这样客服不会被大屏指标分散注意力，管理员也不会把高风险审批混在政策发布里。三端的共同事实源是工单、Proposal、审计和运行记录，前端显示不是权限控制，关键接口仍在服务端按角色校验。

#### 3. 你如何界定这个项目不做什么？

**口播：**我刻意不把它扩展成通用 Agent 平台，也不接真实支付、库存、退款或客户隐私数据。原因是作品集阶段最容易为了堆技术名词而丢掉可验证性：如果既做多 Agent、实时抓取、任意工作流和生产写入，就很难给每个风险点建立可复盘证据。我优先保证两条旗舰链路：物流延迟的低风险处置，以及商品破损/缺件的人审 Proposal。语音、Redis、复杂多 Agent 和线上部署都只能在有明确问题、对照指标与安全条件后再进入下一版，不能因为计划里写过就说已经实现。

**主题追问 1：为什么 Ops 不做完整 BI 大屏？**  
**口播：**我把 Ops 的首要任务定义为“当前这张工单怎么处置”，所以只保留待审批、超时风险等紧凑的 Shift Health 信息。详细的 Policy 命中、工具失败、成本、延迟和坏例趋势放到 Admin Evaluation。这样做的取舍是 Ops 页面少一些宏观图表，但客服在查看 Proposal 时能连续看到订单、物流、图片、政策依据和审批动作，减少在分析页面和工单页面间来回切换。后续如果真实运营团队提出排班或积压管理需求，再以真实使用场景决定是否增加运营仪表盘，而不是预先假设所有指标都应堆在 Ops。

**主题追问 2：项目和 Wayly 如何形成组合，而不显得分散？**  
**口播：**我不把两个项目理解为两个行业故事，而是同一条“可靠 AI 应用”能力线的两个侧面。Wayly 面向消费者，重点是约束推荐、验证和人工复核；CarePilot 面向企业流程，重点是模型如何在工具、状态、权限和人审约束下推进任务。它们共同的底层方法是：模型不拥有最终事实和最终权限，结果要有数据、规则或人工证据兜底。区别是 CarePilot 更能展示 Agentic workflow、Tool calling、运营工作台和审计评测，这正好补足纯推荐产品在企业执行环节的表达。

### B. 安全工作流与 Agent 运行

#### 4. 为什么高风险动作不能只靠 Prompt 限制？

**口播：**Prompt 只能影响模型倾向，不能成为资金、履约或权益变化的最终控制点。模型可能误解上下文、被诱导、输出不符合预期，或者未来替换模型后行为发生变化。因此我让模型最多提出 `refund.propose`、`replace.propose` 等建议，然后把最终权限放在确定性 Permission Engine：高风险 Proposal 一律是 `REQUIRE_HUMAN`，工单进入 `WAITING_REVIEW`，客服才能同意、修改、不采用或接手。运行时没有退款、换货等高风险 execute Tool 暴露给模型。这样即使模型生成不理想内容，业务写入边界仍由服务端守住，并且每一步会写入审计记录。

#### 5. 一张真实 Agent 工单的主链路是什么？

**口播：**工单创建后先进入处理状态，服务端读取当前激活的模型配置和 Tool 白名单，模型必须依次获取订单、物流和已发布政策；有图片时还必须读取已抽取的 Evidence。模型返回受 JSON schema 约束的意图、方案、状态建议、政策引用和用户话术。服务端会检查必需 Tool 是否实际成功调用，再把输入输出、Tool trace、政策证据、token、耗时和成本持久化。最后 Permission Engine 独立判断：高风险 Proposal 去人工审核，证据或政策不足则请求补充材料，只有低风险方案才创建跟进任务并通知用户。模型不直接写工单终态。

#### 6. 为什么要限制 Tool 轮次并处理 DeepSeek 的无状态调用？

**口播：**Agent 的价值来自工具，但无限循环会带来成本、延迟和重复执行风险。因此我把 Tool loop 限制为最多八轮，并要求成功的同参调用不能重复；超过限制会显式失败，而不是悄悄给一个看似正常的答案。实现时还发现兼容 Responses API 的 DeepSeek 路径是无状态的，不能依赖 OpenAI 风格的 `previous_response_id`。所以服务端要把系统提示、用户上下文、函数调用和函数结果完整重放到下一轮。这个处理体现了我把提供方差异放在运行时适配层，而不是让业务状态机依赖某个模型厂商的隐式会话行为。

**主题追问 1：模型没有配置时，系统为什么不回退到 fixture？**  
**口播：**因为静默回退会制造最危险的假象：界面仍然给出结果，但使用者会误以为那是模型判断。真实运行模式中，如果没有服务端 Key，系统会把运行标记为 `REAL_MODEL_NOT_CONFIGURED` 并进入失败路径，同时记录失败类型。fixture 模拟仅在明确的 `FIXTURE_SIMULATION` 路径使用，且文档和页面都标识为离线模拟。这样做短期会降低 Demo 的“顺滑感”，但它保护了项目最重要的证据边界：真实模型效果、规则结果和固定样本标注必须可区分、可追溯。

**主题追问 2：为什么不用 Multi-Agent 作为核心亮点？**  
**口播：**我把 Multi-Agent 看成应由实验决定的实现选项，不是默认正确答案。售后流程本身已经有订单、物流、政策、证据和权限几个边界，如果单 Agent 加受限工具能稳定完成、成本更低，拆成多个 Agent 反而会增加调度、观测和失败归因的复杂度。因此当前核心是单 Agent 的有界工作流；只有在固定数据、Prompt、政策和成本口径下，多 Agent 能带来明确的处理质量增益且不降低安全指标，我才会把它设为默认架构。这个取舍比为了简历堆多 Agent 名词更可解释。

### C. 政策、图片与事实依据

#### 7. 如何避免模型编造售后政策？

**口播：**我没有让模型凭训练知识回答政策，而是要求它在处理前调用只查询 Active 快照的 `policy.search`，并且 Proposal 只能引用工具返回的 chunk。政策来源不是由前端任意 URL 抓取，而是受控来源包导入为快照，保存发布方、URL、时间、摘录和 checksum。新内容先形成 Candidate，回归通过后仍需管理员人工发布，才能替换 Active。这样模型“会说什么”与当前发布的政策证据绑定；同时我也明确公开来源只能作为受控检索参考，不代表具体订单的法律适用性，避免把技术流程包装成法律判断。

#### 8. Stage 20 为什么要修复检索归一化，而不是只改 Prompt？

**口播：**真实评测中的坏例显示，部分零命中不是 Resolution Prompt 不会表达，而是检索层只按字面关键词匹配，模型使用的售后同义词没有命中已发布政策。我的处理不是让模型更强硬地“猜政策”，而是在检索入口做有限同义词归一化，并以订单工具给出的规范品类为准；同时把模型传入的 query、实际 query 和实际 category 一并写入 Tool output。这样我们能区分模型调用问题和检索执行问题。v0.2.2 的 Policy Retrieval 达到 100%，但我把结论限定在该冻结集，不能说真实政策检索已经普遍可靠。

#### 9. 图片 Evidence 为什么不能直接决定换货？

**口播：**图片模型能做的是提取可见事实，例如破损、缺件、包装状态、清晰度和图文匹配度；它不应该判断退款资格或给用户贴欺诈标签。真实评测还暴露出一个关键问题：同一张图片可能真实地展示了破损，但它与订单商品不匹配，因此不能拿视觉准确率替代订单级证据充分性。我把模型原始的人工复核判断单独保存，再用图片质量、订单/诉求匹配、置信度和必要可见字段执行确定性门禁。最后即使图片充分，也只支持生成 `replace.propose` 并进入人工审核，不会触发自动换货。

**主题追问 1：Stage 20 的 3/3 配对图片观察和 2/3 是什么含义？**  
**口播：**我会先解释分母，避免只报一个漂亮百分比。v0.2.2 新增了三条订单和图片已经配对的高风险样本，用来把“图片本身的视觉判断”与“订单—图片是否匹配”分开。三条图片的可见事实观察都符合冻结预期；其中两条的模型复核判断和订单级证据充分性也符合预期。第三条识别到包装、数据线和未见手机本体，但模型仍认为单图不足以证明内容完整，因此主动要求复核。这个案例没有被我用 Prompt 强行调成通过，因为保守转人工在权益场景中是合理行为，应作为坏例继续分析而非被隐藏。

**主题追问 2：公开政策来源的边界是什么？**  
**口播：**公开政策来源解决的是“模型回答时能否给出可审计的受控依据”，并不自动解决来源实时性、条款解释、地区差异或具体订单的法律适用性。我保存 URL、发布方、版本、导入时间和 checksum，并要求新增来源走 Candidate、回归和人工发布，减少静默变更风险。但仓库的订单、物流、支付、库存和客户信息仍是脱敏模拟数据，检索包也不是任意网页的实时爬虫。因此面试时我会说这是 policy grounding 的技术与产品治理实践，不会说已经实现了法律级合规判断或真实企业政策运营。

### D. 评测、版本和坏例

#### 10. 80 条离线固定评测集有什么用，又有什么局限？

**口播：**80 条固定 case 的作用是把以前代码里的汇总段数据变成带稳定 ID、品类、风险、异常、预期状态和 Proposal 的可执行输入。它覆盖数码、服饰、家居 32/24/24，包含高风险和提示注入、超时、重复请求、政策冲突等异常，能验证 fixture 自身完整性、状态机契约和回归不会被误改。但它不代表我让真实模型跑完了 80 条，也不表示图片和检索已经达到生产精度。尤其 `has_image` 最初只是测试路径标志，不等于有一张真实图片或真实视觉结果，所以我会把它称为固定模拟输入与确定性验证，而不是模型效果。

#### 11. 真实模型冻结评测怎样做到可比较？

**口播：**真实评测不是每次点一次按钮就比较分数。我把 fixture、图片哈希、模型、Prompt 版本和 Active 政策快照都记录并校验；如果当前配置与评测基线不一致，接口会阻止直接把结果混入旧版本。每条运行保存输入输出、完整 Tool trace、政策证据、耗时、token、估算成本、失败类型和坏例，汇总表也保留相同字段。这样当 v0.2.1 出现 Policy 命中和图片复核问题时，才能定位是 Tool 轮次、检索字面匹配、订单图片不匹配还是视觉模型判断，而不是只说“模型不稳定”。

#### 12. 你从 v0.2 到 v0.2.2 学到了什么？

**口播：**我认为最有价值的不是把某个数字调高，而是保留失败轨迹。v0.2 中有 case 因旧的四轮 Tool 限制失败；v0.2.1 修复到八轮后恢复了该 case，但高风险验收和图片复核仍不够好。继续分析后发现，一部分高风险失败来自订单和图片不匹配，另一部分 Policy 零命中来自检索词归一化不足。v0.2.2 因此只做最小修复：有限同义词归一化、订单品类约束和 Evidence 两层门禁，并新建版本而不覆盖旧运行。这样的迭代让我能解释因果和边界，而不是把历史坏例删掉。

**主题追问 1：Manual vs Agent 的实验结果能写进简历吗？**  
**口播：**我会非常谨慎地写。现有数据是五个匿名槽位、二十条完全模拟任务的描述性观察：Agent-assisted 条件平均记录时间较短、信息操作较少，两组都达到冻结目标状态，高风险门槛没有被绕过。但样本很小，同一场景对由不同槽位完成，参与者差异会混入结果；而且 Agent-assisted 页面可见工具入口本来就更少，信息操作数不能直接解释为认知负荷或生产力。因此我最多写“完成小样本模拟可用性观察并记录结果和限制”，不会写“客服效率提升 15.7%”或“已验证业务收益”。

**主题追问 2：为什么要同时保留离线、模拟真人和真实模型三种证据？**  
**口播：**因为它们回答的问题不同。离线 fixture 适合稳定验证状态、风险和异常契约；匿名模拟任务适合观察人在不同界面条件下的完成路径和主观感受；真实模型冻结集才验证模型、工具调用、政策引用和视觉路径实际发生。把它们混成一个总分会掩盖关键问题，比如规则契约通过不代表模型会调用正确工具，模型输出合格也不代表用户体验更好。我在页面和文档中保留 execution mode、fixture 名称、运行 ID 与 source note，就是为了让每类数字都能追溯到正确的证据等级。

### E. 鉴权、责任边界与下一步

#### 13. 前端做了角色路由后，为什么还需要服务端 RBAC？

**口播：**前端路由或隐藏菜单只影响用户看到什么，不能阻止绕过页面直接调用 API。因此我用 Supabase Auth 的 Bearer JWT 和 JWKS 验签获取身份，再从受控 `profiles` 读取 `user`、`admin`、`superadmin` 三类角色。普通用户只能读取自己的模拟订单和工单并提交材料；客服处理工单；只有超级管理员能改政策、配置和运行评测。所有关键接口都依赖服务端 `require_roles`，不是信任浏览器传来的 reviewer 字符串。这个设计的边界是身份和权限链已接入，但业务数据仍是受控脱敏演示数据，不应该被说成真实生产多租户系统。

#### 14. 你如何处理“真实模型”与“真实业务”的边界？

**口播：**我会把两句话拆开说：项目已经有真实服务端模型调用和持久化的 `REAL_AGENT_RUN`，但没有接入真实订单、物流、支付、库存或客户数据，也没有线上客服团队使用。真实模型意味着模型 API、真实 Tool loop、视觉调用、token、延迟、成本和坏例确实发生；真实业务则还需要数据授权、生产 API、监控、隐私生命周期、组织流程和长期效果验证。为了防止两者混淆，系统把模型未配置、运行失败、fixture 模拟和真实运行显式标记，而不是让界面统一显示为“AI 已处理”。

#### 15. 如果继续迭代，你下一步会做什么，为什么？

**口播：**我不会先扩展更多风险规则、页面或多 Agent，而是围绕当前真实坏例做新版本验证。优先级第一是继续提高高风险验收样本的代表性，保证图片与订单确实配对，并分析为何某些证据充分场景仍被模型要求补件；第二是以新冻结版本评估检索同义词和政策引用的泛化，而不是只在现有样本上调分；第三才是在合规授权和部署边界清晰后，考虑真实服务端运行环境和更大规模的用户研究。每一步都要冻结输入、记录模型/Prompt/政策快照、保留失败样本，并提前定义什么结果才值得扩大范围。

**主题追问 1：你的个人职责如何讲才不夸大？**  
**口播：**我会先说明我负责的是产品范围和证据边界：确定售后场景、三端职责、高风险必须人审、评测分层，以及哪些模拟数据不能包装成生产结果。在实现上，我会如实说明是在 AI 辅助下完成前端、FastAPI、数据迁移、测试和工作日志的协作开发与验证，而不是把自己描述成真实客服团队的唯一工程 Owner。对真实模型实测、公开政策包和图片样本，我只陈述已有运行记录、受控来源和项目负责人确认的范围。这样面试官能继续深挖我的决策与验证过程，而不会因为过度 Ownership Claim 失去可信度。

**主题追问 2：你如何定义项目“完成”？**  
**口播：**我不把页面全部做完或模型跑通一次当作完成。对当前作品集版本，完成应至少同时满足：真实模型运行和离线模拟清晰可区分；高风险工具不可能被模型直接执行；模型、Prompt、工具、政策和 Evidence 能被审计；冻结评测有结果和坏例；用户研究结果与生产收益严格区分。对于更高一级的上线完成，还需要真实业务数据授权、正式服务端部署、隐私和删除策略、监控告警、故障演练以及真实团队验收。把完成条件分层可以避免用“Demo 已跑通”代替真正的产品就绪度。

## 4. 源码与证据索引

| 主题 | 关键路径与内部符号 | 对应正文位置 |
| --- | --- | --- |
| 产品范围与三端 | `PLAN.md`、`apps/web/app/consumer/page.tsx`、`page.tsx`、`admin/page.tsx` | 问题 1–3 |
| 状态/权限/审批 | `apps/api/app/domain.py`：`transition`、`permission_for_resolution`、`process_real_ticket`、`review_ticket` | 问题 4–6 |
| 模型与工具 | `apps/api/app/agent_runtime.py`：`ResolutionOutput`、`OpenAIResponsesRuntime`、`MAX_AGENT_TOOL_ROUNDS` | 问题 5–6 |
| 政策 grounding | `data/005_public_policy_grounding.sql`、`domain.py`：`search_active_public_policy`、`normalize_policy_query` | 问题 7–8 |
| 视觉与 Evidence 闸门 | `data/009_real_vision_evidence_origin.sql`、`data/010_evidence_review_gate.sql`、`domain.py`：`evidence_review_gate` | 问题 9、12 |
| 评测 | `evals/README.md`、`evals/manual-agent-results-v0.2.md`、`evals/fixtures/real-agent-eval-v0.2.2.json`、Stage 18–20 | 问题 10–12 |
| 鉴权 | `data/008_supabase_auth_rbac.sql`、`apps/api/app/auth.py`、`apps/web/lib/carepilot-auth.ts` | 问题 13–14 |
| 回归证据 | `apps/api/tests/test_ticket_flow.py`、`docs/worklog/2026-09-11-stage-20-evidence-retrieval-repair.md` | 全文 |

## 5. 交给 /great-resume 的项目事实摘要

- **项目与岗位：** CarePilot（售后智策），面向 AI 应用/产品经理岗位；展示企业工作流、Agent、安全边界、评测和人机协同。
- **个人职责边界：** 项目负责人确定产品范围、风险/证据边界与样本确认；前后端、数据迁移、测试与文档为 AI 辅助协作实现。不要写成真实客服团队生产 Owner。
- **关键动作：** 定义三端与状态机；实现真实模型结构化 Tool loop；将权限从 Prompt 中剥离；将政策与 Evidence 分层审计；冻结并复盘真实模型坏例。
- **可核验证据：** 80 条离线 fixture、5 槽位/20 条匿名模拟记录、20 张视觉冻结图、15 样本 v0.2.2 真实运行、持久化 AgentRun/Policy/Evidence 结构、后端单测与 Web build 记录。
- **可写候选表述：** 优先使用本文件的五条 bullet，并保留“受控脱敏评测”“小样本模拟”限定语。
- **待补事实：** 真实客服用户研究、生产部署、真实业务 KPI、真实系统集成、长期稳定性与政策法律适用性。

## 6. 交给 /interview 的高风险 Claim 清单

- **Ownership Claim：** “主导全部工程”“独立上线”“真实客服团队 Owner”均无仓库证据；可说产品范围和边界由项目负责人确定，工程为 AI 辅助协作实现。
- **Metric Claim：** 18.3 秒/21.7 秒、100% Policy Retrieval、10/10 安全阻断都限定于特定模拟或冻结评测；不能外推真实客服效率或业务增长。
- **Architecture Claim：** 可以说已有真实服务端 Tool loop、RBAC、审计与权限门禁；不能说已实现 Redis、RQ、Multi-Agent 默认架构、完整向量 RAG 或生产级 Storage 生命周期。
- **Result Claim：** 可以说真实模型技术评测和受控模型运行已记录；不能说生产上线、真实电商接入、GMV/满意度/留存提升或法律合规已验证。
