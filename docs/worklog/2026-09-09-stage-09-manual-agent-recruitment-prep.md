# 阶段九工作记录：Manual vs Agent 招募前执行准备

- 记录日期：2026-09-09
- 对应计划：`PLAN.md` 的 5 人 Manual vs Agent 交叉平衡实验前置工作
- 交付阶段：**匿名任务分配与参与者执行说明已准备**；未招募参与者、未收集记录、未产生实验结论

## 1. 目标

将阶段七的 10 对 / 20 条任务变为可被独立检查的 5 槽位分配，避免同一参与者同时完成同一场景对的两个条件，并在开始任何真人测试前明确数据和停止边界。

## 2. 实现

- 新增 `evals/fixtures/manual-agent-assignment-v0.1.json`：`P01` 至 `P05` 为待招募的匿名槽位，不对应真实身份。每个槽位含 4 条任务，`MANUAL` 与 `AGENT_ASSISTED` 各 2 条。
- 20 条条件任务均仅出现一次；每个场景对的两个条件分给不同槽位。四轮的条件数量为 3:2、2:3、3:2、2:3，降低固定先后顺序的学习效应。
- 新增 `evals/manual-agent-participant-kit-v0.1.md`：提供统一说明、只用模拟数据的前置条件、观察者记录范围、高风险人工审批要求与停止条件。
- 后端加载器校验任务覆盖、每槽位数量与条件平衡、轮次完整性、轮次条件平衡以及同一场景对隔离。

## 3. 验证证据

| 检查 | 结果 | 性质 |
| --- | --- | --- |
| `apps/api/.venv/bin/python -m unittest discover -s tests -v` | 12 / 12 通过 | 本地任务、分配、Evidence 与 API 回归验证 |
| 分配完整性 | 5 个匿名槽位、20 / 20 条任务各一次、每槽位 4 条 | 自构造分配输入技术验证 |
| 平衡与隔离 | 每槽位两种条件各 2 条；同场景对拆分给不同槽位；各轮差异不超过 1 条 | 自构造交叉平衡技术验证 |

## 4. 责任与边界

- `P01` 至 `P05` 不是已招募参与者，assignment 文件不是实验数据或用户画像。
- 当前没有任务开始/结束时间、正确性、步骤数、Proposal 修改、接管、评分或观察备注，因此没有任何效率、准确性、满意度或业务效果结论。
- 本执行包不判断课程、学校、机构或招募渠道是否还要求审批；项目负责人须在实际招募前按适用规则确认。

## 5. 下一步

1. 项目负责人确认可开展仅使用模拟数据的 5 人测试后，按参与者执行包分配匿名槽位并逐条记录；不得补造或回填结果。
2. 在具备合法来源、Storage、签名 URL、鉴权和删除策略前，不接入真实图片或真实售后数据。
