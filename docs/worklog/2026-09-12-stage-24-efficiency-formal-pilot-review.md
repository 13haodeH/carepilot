# 阶段二十四工作记录：客服提效首轮正式样本复盘与计时修复

- 记录日期：2026-09-12
- 数据范围：Supabase `FORMAL` 模式中完成全部 6 题的匿名参与者；查询快照为 6 人、36 条记录。
- 交付阶段：**小样本描述性复盘与下一轮计时口径修复；不是客服效率效果结论。**

## 1. 输入与核验

- 仅纳入 `customer_efficiency_participants.completed_at` 非空的正式参与者，排除未作答与中途退出会话。
- 6 名完整匿名参与者各完成 3 道 Manual 与 3 道 Decision Package，共 18 / 18 条记录；没有重复提交。
- `duration_seconds` 是服务端从“开始做题”至“提交这一题”的墙钟时长。它此前没有区分浏览器在后台、离开页面或暂停作答的时间。

## 2. 描述性观察

| 指标 | Manual（18 题） | Decision Package（18 题） | 当前可作的解释 |
| --- | ---: | ---: | --- |
| 平均墙钟时长 | 40.8 秒 | 316.9 秒 | 不可作为提效结论；Decision Package 有 1 条 4,769 秒的长时记录。 |
| 中位墙钟时长 | 32.5 秒 | 64.0 秒 | 即使排除长时记录，Decision Package 平均 55.1 秒、中位 59.0 秒，当前没有时间优势。 |
| 平均打开原始信息次数 | 2.06 | 2.28 | 目前未观察到额外核验减少。 |
| 决策前信息完整度 | 55.4% | 95.4% | 这是 Decision Package 预先整理信息的直接产物，说明“决策准备度”更高，但不能单独证明效率。 |
| 符合冻结处置口径 | 14 / 18 | 15 / 18 | 差异很小，不能作统计推断。 |
| 易用性评分（1–7） | 6.28 | 5.89 | 当前没有易用性优势。 |

高风险任务共 14 条，全部保留“交给人工进一步确认”的门槛（14 / 14）。其中 6 条 Decision Package 高风险任务中，3 条按建议处理、1 条修改建议、2 条由参与者接手；这说明系统建议应继续被设计为“人工决策准备材料”，不能包装成自动决策。

## 3. 问题与决策

### 问题一：墙钟时长会被离开页面污染

`EFF-01` 的一条低风险 Decision Package 记录为 4,769 秒，显著拉高均值。该值可能包含离开页面或暂时中断，不能直接等同于处理时长。

**决策：** 保留历史 `duration_seconds`，不修改也不剔除已有正式记录；为后续记录新增可空的 `active_duration_seconds`。公开页仅在浏览器处于前台时累积该值；后端拒绝超过服务器墙钟时长的上报。历史记录保持 `NULL`，分析时必须按计时版本分开。

### 问题二：首轮样本尚未呈现 Decision Package 的时间或核验优势

在 6 个完整样本中，Decision Package 的中位时间、额外核验次数和易用性均没有优于 Manual。可能原因包括样本小、题目/条件覆盖不均、参与者阅读更长的系统整理信息，以及当前页面没有把“先看摘要、再按需复核”的路径讲得足够清楚。

**决策：** 不修改题库、目标处置或高风险门槛来追逐指标；先修复计时口径，继续招募至至少 8 名完整参与者，并同时检查每个 pair / 条件覆盖，再判断是否需要做一次受控的页面文案或信息层级迭代。

## 4. 验证与边界

- 本阶段只汇总正式匿名记录；没有收集姓名、联系方式、真实订单、图片或客服业务结果。
- 这些是自构造模拟题的过程数据，不支持外推为生产 SLA、客服成本、GMV、满意度或真实自动处置覆盖率。
- `active_duration_seconds` 为浏览器端的前台时间近似值，不是受控实验室的精确工时；仍需与服务器墙钟时长并列保存。

## 5. 下一步

1. 执行 `data/012_customer_efficiency_active_time.sql`，部署 API 与公开页，并以新参与者记录核验该字段。
2. 招募至至少 8 名完整参与者；报告中分开呈现 v1.0 墙钟时长与 v1.1 前台作答时长。
3. 达到样本与覆盖门槛后，复核长时值、Manual / Decision Package 的配对覆盖、信息操作、正确处置与高风险建议采纳；再决定是否迭代页面信息层级。
4. 只有出现完整、可追溯且与口径一致的结果后，才更新 Evaluation 首屏、Demo 讲稿和简历项目表述。
