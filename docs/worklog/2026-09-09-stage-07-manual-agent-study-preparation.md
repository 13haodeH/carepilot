# 阶段七工作记录：Manual vs Agent 实验准备

- 记录日期：2026-09-09
- 对应计划：`PLAN.md` 的 20 条 Manual vs Agent 对照任务与 5 人实验前置工作
- 交付阶段：**自构造任务协议与校验**；未招募参与者、未开展可用性测试、未产生实验结果

## 1. 目标

为后续实验定义 10 个难度匹配的业务场景对和 20 条条件任务，使 Manual 与 Agent-assisted 条件能比较任务完成时间、实际参与者操作、正确性、Proposal 修改和接管情况。

## 2. 实现

- 新增 `evals/fixtures/manual-agent-tasks-v0.1.json`：10 个场景对、20 条唯一任务，数码 / 服饰 / 家居为 4 / 3 / 3 对。
- 每个场景对包含相同目标的 `MANUAL` 与 `AGENT_ASSISTED` 条件；同一参与者不应完成同一对的两个条件。
- 3 条高风险场景在两种条件中均要求 `proposal_review`，不因 Agent 辅助而绕过人工审批。
- 新增 `evals/manual-agent-study-v0.1.md`：定义交叉平衡、匿名 participant ID、记录字段、停止规则和禁止主张的边界。
- 后端加载器验证配对数、任务 ID、品类分布、目标状态、原子步骤引用和高风险人工闸门。

## 3. 验证证据

| 检查 | 结果 | 性质 |
| --- | --- | --- |
| `apps/api/.venv/bin/python -m unittest discover -s tests -v` | 12 / 12 通过 | 本地任务 fixture 与 API 回归验证 |
| 任务覆盖 | 10 对 / 20 条任务；3 条高风险对均包含人工 Proposal 审核 | 自构造实验输入技术验证 |

## 4. 责任与边界

- 任务、订单、政策和操作步骤均为自构造模拟；不包含真实用户、客户资料或线上操作。
- 当前没有参与者、开始/结束时间、正确率、SUS、步骤数或处理时长记录。
- 因此不能主张任何 Manual vs Agent 效率、准确性、满意度或业务效果结论。

## 5. 下一步

1. 招募前参与者说明和交叉平衡分配已准备；是否开展仅使用模拟数据的 5 人测试由项目负责人确认。
2. 图片 Evidence 的自构造测试图、MIME / 大小 / EXIF 处理与人工复核链路已完成；开始真人实验前仍不得把模拟标注表述为真实图片效果。
3. Policy 回归闸门的远程迁移与失败发布拦截已完成；开始实验前保持其为模拟开发库验证，不将其表述为真实政策发布。
